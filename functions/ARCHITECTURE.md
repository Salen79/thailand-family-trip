# 🏗️ Архитектура Cloud Functions для Telegram уведомлений

## Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────┐
│                   React Приложение (spa-app)                │
│                    DiaryScreen.tsx                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ Пользователь создает запись
                      ▼
┌─────────────────────────────────────────────────────────────┐
│               Firestore Database                             │
│              Collection: diary_posts                         │
│  {                                                           │
│    title: "Новая запись",                                   │
│    content: "Содержание...",                                │
│    author: "Иван",                                          │
│    createdAt: Timestamp                                     │
│  }                                                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ Триггер onCreate
                      ▼
┌─────────────────────────────────────────────────────────────┐
│          Firebase Cloud Functions v2                         │
│        onDiaryPostCreated (functions/src/index.ts)          │
│                                                              │
│  1. Получает данные из DocumentSnapshot                     │
│  2. Форматирует сообщение                                   │
│  3. Экранирует HTML символы                                 │
│  4. Вызывает sendTelegramMessage()                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ HTTP POST
                      ▼
┌─────────────────────────────────────────────────────────────┐
│            Telegram Bot API                                  │
│  https://api.telegram.org/bot{TOKEN}/sendMessage            │
│                                                              │
│  POST /sendMessage {                                        │
│    chat_id: "-5180269549",                                  │
│    text: "📝 <b>Новая запись</b>\n...",                    │
│    parse_mode: "HTML"                                       │
│  }                                                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│              Telegram Group                                  │
│          -5180269549 (Thailand Trip)                         │
│                                                              │
│  📝 Новая запись                                            │
│  Автор: Иван                                                │
│  Дата: 30.12.2025                                           │
│                                                              │
│  Содержание записи обрезано до 200 символов...             │
│                                                              │
│  #diarypost #doc123abc                                      │
└─────────────────────────────────────────────────────────────┘
```

## Структура файлов

```
functions/
│
├── src/
│   ├── index.ts                    # 🔴 Основная функция (Firebase Cloud Function)
│   │   └── export const onDiaryPostCreated
│   │       └── Слушает: collection("diary_posts").onCreate()
│   │       └── Форматирует сообщение
│   │       └── Вызывает sendTelegramMessage()
│   │
│   ├── telegram.ts                 # 📤 Отправка в Telegram API
│   │   └── export async function sendTelegramMessage(message)
│   │       └── Получает конфиг (токен, ID чата)
│   │       └── Отправляет HTTP POST в Telegram API
│   │       └── Логирует результат
│   │
│   └── config.ts                   # ⚙️ Управление конфигурацией
│       ├── export function getConfig()
│       │   └── Возвращает { telegramBotToken, telegramChatId }
│       │   └── Приоритет: process.env > functions.config()
│       │
│       └── export function validateConfig()
│           └── Проверяет наличие обязательных переменных
│           └── Выбрасывает ошибку если конфиг отсутствует
│
├── lib/                            # 📦 Скомпилированный JavaScript
│   ├── index.js
│   ├── telegram.js
│   └── config.js
│
├── tsconfig.json                   # TypeScript конфигурация
├── package.json                    # npm зависимости и скрипты
├── .env.local                      # 🔐 Локальные переменные (не коммитится)
└── .gitignore                      # Исключить из Git

firebase.json                        # Конфигурация Firebase проекта
├── functions:
│   ├── source: "functions"         # Исходная папка
│   ├── runtime: "nodejs20"         # Node.js версия
│   └── codebase: "default"
```

## Поток выполнения

### 1. Создание документа в Firestore

```typescript
// spa-app/src/screens/DiaryScreen.tsx
const docRef = await addDoc(collection(db, "diary_posts"), {
  title: "Тестовая запись",
  content: "Содержание...",
  author: "Иван",
  createdAt: Timestamp.now()
});
// ✅ Документ создан
```

### 2. Триггер Cloud Function

```typescript
// functions/src/index.ts
export const onDiaryPostCreated = functions.firestore.onDocumentCreated(
  "diary_posts/{postId}",  // ← Путь к коллекции
  async (event) => {       // ← Срабатывает автоматически
    // Получаем данные документа
    const postData = event.data!.data();
    const postId = event.params.postId;
```

### 3. Форматирование сообщения

```typescript
// functions/src/index.ts
const message = formatDiaryPostMessage(postData, postId);
// Результат:
// "📝 <b>Тестовая запись</b>\n
//  <b>Автор:</b> Иван\n
//  <b>Дата:</b> 30.12.2025\n\n
//  Содержание...\n\n
//  <i>#diarypost #doc123</i>"
```

### 4. Отправка в Telegram

```typescript
// functions/src/telegram.ts
await sendTelegramMessage(message);

// Внутри:
// 1. Получает токен и ID из конфигурации
// 2. Отправляет HTTP POST:
//    POST https://api.telegram.org/botXXX/sendMessage
//    { chat_id: -5180269549, text: message, parse_mode: "HTML" }
// 3. Логирует результат
```

### 5. Результат в Telegram

```
📝 Тестовая запись
Автор: Иван
Дата: 30.12.2025

Содержание...

#diarypost #doc123
```

## Управление конфигурацией

### Для локальной разработки

```bash
# .env.local
TELEGRAM_BOT_TOKEN=8110733520:AAHmTrG8NXm1DwFeSrA5POaiDpgu2gKHaUk
TELEGRAM_CHAT_ID=-5180269549

# Загружается через dotenv автоматически
# Доступно как process.env.TELEGRAM_BOT_TOKEN
```

### Для production в Firebase

```bash
# Одноразовая установка
firebase functions:config:set telegram.bot_token="8110733520:..."
firebase functions:config:set telegram.chat_id="-5180269549"

# Доступно как функция.config().telegram.bot_token
# Но код приоритизирует process.env если доступна
```

## Обработка ошибок

```typescript
try {
  // 1. Проверяем конфигурацию
  validateConfig();
  
  // 2. Отправляем сообщение в Telegram
  const response = await axios.post(telegramApiUrl, {...});
  
  // 3. Проверяем ответ
  if (!response.data.ok) {
    throw new Error(`Telegram error: ${response.data.description}`);
  }
  
  // 4. Логируем успех
  functions.logger.log("Message sent", { messageId: response.data.result.message_id });
  
} catch (error) {
  // 5. Логируем ошибку
  functions.logger.error("Failed to send message", { error: error.message });
  // 6. Выбрасываем ошибку (Firebase будет повторять попытку)
  throw error;
}
```

## Безопасность

### ✅ Что защищено

| Компонент | Защита |
|-----------|--------|
| Токен бота | Хранится в Firebase конфигурации, не в коде |
| ID чата | Хранится в Firebase конфигурации, не в коде |
| HTML контент | Экранируется перед отправкой в Telegram |
| Логирование | Не логируются чувствительные данные |
| Доступ к функции | Контролируется правилами Firestore |

### 🔐 Как работает конфигурация

```
┌─────────────────────────────────────────┐
│  Источники конфигурации (приоритет)     │
├─────────────────────────────────────────┤
│ 1️⃣ process.env (самый высокий)        │
│    TELEGRAM_BOT_TOKEN                   │
│    TELEGRAM_CHAT_ID                     │
├─────────────────────────────────────────┤
│ 2️⃣ functions.config()                  │
│    functions.config().telegram.bot_token│
│    functions.config().telegram.chat_id  │
├─────────────────────────────────────────┤
│ 3️⃣ Ошибка если ничего не найдено      │
│    "Missing TELEGRAM_BOT_TOKEN..."      │
└─────────────────────────────────────────┘
```

## Масштабируемость

```
Один документ dibrary_posts → Одна Cloud Function вызов
     ↓
Множество документов → Множество параллельных вызовов
     ↓
Каждый вызов отправляет отдельный HTTP запрос в Telegram
     ↓
Telegram обрабатывает параллельные запросы (лимит: 30 сообщений/сек)
```

## Затраты

- ✅ Cloud Functions: 2 млн бесплатных вызовов/месяц
- ✅ Firestore: 50 тыс операций чтения/записи в день (бесплатно)
- ✅ Telegram API: Абсолютно бесплатно
- **Итого**: Практически ноль затрат при типичном использовании

## Интеграция с React

```
spa-app/src/screens/DiaryScreen.tsx
│
├── import { createDiaryPost } from "../services/diaryService"
│
├── async function handleSubmit() {
│   const docId = await createDiaryPost(title, content, author)
│   // ↓
│   // Firestore добавляет документ
│   // ↓
│   // Cloud Function автоматически срабатывает
│   // ↓
│   // Telegram получает сообщение
│   showSuccess("✅ Запись добавлена в Telegram!")
│ }
```

## Мониторинг и логирование

```bash
# Просмотреть все логи функции
firebase functions:log

# Смотреть логи в реальном времени
firebase functions:log --follow

# Фильтр по функции
firebase functions:log | grep "onDiaryPostCreated"

# Firebase Console
# → Functions → Logs → onDiaryPostCreated
```

Типичные логи:
```
i  functions: New diary post created {postId: "abc123", data: {...}}
✔  functions: Telegram message sent successfully {messageId: 9876}
```

## Расширение функциональности

### Добавить уведомления для других событий

```typescript
// functions/src/index.ts

// Уведомление при обновлении записи
export const onDiaryPostUpdated = functions.firestore.onDocumentUpdated(
  "diary_posts/{postId}",
  async (event) => {
    // Код для обновления
  }
);

// Уведомление при удалении записи
export const onDiaryPostDeleted = functions.firestore.onDocumentDeleted(
  "diary_posts/{postId}",
  async (event) => {
    // Код для удаления
  }
);
```

### Кастомизация сообщения

Измени функцию `formatDiaryPostMessage()` в `functions/src/index.ts`:

```typescript
function formatDiaryPostMessage(...) {
  return (
    `🆕 <b>${title}</b>\n` +
    `👤 ${author}\n` +
    `📅 ${date}\n\n` +
    `${content}\n\n` +
    `#diary #post`
  );
}
```

---

**Вывод:** Полностью автоматическая, безопасная и масштабируемая система для отправки уведомлений в Telegram при создании новых записей в Firestore! 🎉
