# 🚀 Быстрый старт - Telegram уведомления для diary_posts

## За 5 минут до production

### 1️⃣ Установить переменные в Firebase (одноразово)

```bash
firebase functions:config:set telegram.bot_token="8110733520:AAHmTrG8NXm1DwFeSrA5POaiDpgu2gKHaUk"
firebase functions:config:set telegram.chat_id="-5180269549"
```

Проверить:
```bash
firebase functions:config:get
```

### 2️⃣ Развернуть Cloud Functions

```bash
firebase deploy --only functions
```

### 3️⃣ Тестировать

Создай документ в Firestore через Console:
- Collection: `diary_posts`
- Поля:
  - `title`: "Тестовая запись"
  - `content`: "Это работает!"
  - `author`: "Тест"
  - `createdAt`: (сегодняшняя дата)

**Результат:** В Telegram группе появится сообщение 📝

---

## 📁 Структура проекта

```
functions/
├── src/
│   ├── index.ts           ← Cloud Function (срабатывает при создании записи)
│   ├── telegram.ts        ← Отправка сообщений в Telegram
│   └── config.ts          ← Конфигурация токенов
├── lib/                   ← Скомпилированный JavaScript
├── tsconfig.json
├── package.json
└── .env.local             ← Локальные переменные (для разработки)
```

---

## 🔄 Как работает

1. Пользователь создает документ в `diary_posts`
2. ↓
3. Cloud Function **автоматически** срабатывает
4. ↓
5. Форматирует сообщение
6. ↓
7. Отправляет в Telegram API
8. ↓
9. 🎉 Сообщение приходит в группу

---

## 📝 Формат сообщения в Telegram

```
📝 Название записи
Автор: Иван Петров
Дата: 30.12.2025

Содержание записи обрезается до 200 символов...

#diarypost #docId123
```

---

## 🔐 Безопасность

✅ **Токены НЕ в коде**
- Хранятся в Firebase конфигурации (`firebase functions:config:set`)
- Загружаются как environment переменные

✅ **HTML экранируется**
- Защита от XSS в Telegram

✅ **Проверка конфигурации**
- Функция проверяет наличие токенов перед отправкой

---

## 📚 Документация

| Файл | Содержание |
|------|-----------|
| `functions/README.md` | Детальная документация Functions |
| `FIREBASE_SETUP.md` | Полная настройка Firebase |
| `functions/TESTING.md` | Локальное тестирование с эмулятором |
| `INTEGRATION.md` | Интеграция в React приложение |
| `DEPLOYMENT_CHECKLIST.md` | Полный чек-лист развертывания |

---

## 🛠️ Команды

```bash
# Сборка TypeScript
cd functions && npm run build

# Локальное тестирование с эмулятором
npm run serve

# Развернуть только Functions
firebase deploy --only functions

# Просмотреть логи (реальное время)
firebase functions:log --follow

# Список развернутых функций
firebase functions:list

# Полное развертывание (Functions + Hosting + Firestore)
firebase deploy
```

---

## ✨ Статус

- ✅ Cloud Functions реализована и протестирована
- ✅ TypeScript скомпилирован в JavaScript
- ✅ Конфигурация готова к развертыванию
- ✅ Документация полная
- 🚀 Готово к production!

---

## ⚡ Следующие шаги

1. **Развернуть на production:**
   ```bash
   firebase deploy --only functions
   ```

2. **Добавить функцию создания записи в React:**
   - См. `INTEGRATION.md` для примера

3. **Мониторить логи:**
   ```bash
   firebase functions:log --follow
   ```

4. **Наслаждаться уведомлениями в Telegram!** 🎉
