rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /diary/{userId}/{fileName} {
      // Allow uploads from authenticated or test users
      allow write: if true;
      // Allow reads from anywhere (needed for CORS-free access)
      allow read: if true;
    }
  }
}
